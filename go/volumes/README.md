# Env Vars Test Function Template

This function template returns the content of any file on the server passed by
que query string param `v`
This is useful to inspect secrets and config maps on the server

